k9s:
  # use `k9s --write` to manually override this and make destroy changes
  readOnly: true
  ui:
    logoless: true
    # mouse will break terminal highlighting, so for now disable it
    # use full screen mode to easily copy/paste
    enableMouse: false
  logger:
    showTime: true
    textWrap: true
