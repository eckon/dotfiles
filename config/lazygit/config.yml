gui:
  showRandomTip: false
  nerdFontsVersion: '3'
  sidePanelWidth: 0.2
  border: 'single'
  mainPanelSplitMode: 'vertical'

git:
  paging:
    colorArg: 'always'
    pager: 'delta --dark --paging=never'

notARepository: 'skip'
