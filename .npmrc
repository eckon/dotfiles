# Allow installation with peer dependency conflicts
# Required for using latest ESLint v10 with typescript-eslint v8
# (typescript-eslint v8 officially supports ESLint v8-v9 only)
legacy-peer-deps=true
